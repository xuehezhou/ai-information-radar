# AI信息雷达项目体检与优化日志

## 记录范围

- 本日志记录 2026-08-18 对项目进行的只读体检结果。
- 本次没有修改 EXE、数据文件、UI 或运行逻辑。
- 依据：`AI信息雷达.exe` 内嵌模块、`data/` 数据、运行日志、模板、静态资源和 `使用说明.txt`。
- 当前项目没有可用源码提交，部分实现依据 PyInstaller 内嵌字节码和运行产物确认；无法确认的内容不作为确定事实。

## 当前架构

项目是 Windows 桌面应用，主要结构如下：

```text
AI信息雷达.exe
  └─ 本地 Flask 服务
       ├─ 读取 data/daily/*.md
       ├─ 读取 data/read_status.json
       ├─ 渲染首页和日报详情页
       └─ 提供 /api/reports、/api/read/<date>

后台补录线程
  └─ collector
       ├─ Hacker News / TechCrunch / VentureBeat
       ├─ Reddit / arXiv / GitHub
       ├─ Product Hunt / Qwen / DeepSeek
       ├─ 去重、质量、时效、营销过滤
       ├─ 分类和 A/B/C 评分
       └─ 写入 data/realtime/YYYY-MM-DD.json

日报生成器
  └─ 采集素材
       └─ 调用 localhost:8787/v1/messages
            └─ 写入 data/daily/YYYY-MM-DD.md
```

项目没有 PostgreSQL、SQLite、Redis 或向量数据库，Markdown 和 JSON 文件就是当前的数据存储层。

## 当前核心问题

### 1. 时效性问题

- 首页只读取“最新已有日报”，不会在用户打开时主动采集当天数据。
- 首页可能把旧日报显示为“今日日报”。
- 截至本次扫描，日报最新为 `2026-08-10`，实时数据最新为 `2026-08-16`。
- 实时数据 `2026-08-11` 至 `2026-08-16` 的内容高度重复。
- 补录逻辑只检查最早日报到昨天之间的缺失日期，不直接解决当天更新。
- 日报生成依赖本地 `127.0.0.1:8787` 路由器；路由器不可用时，采集成功但日报生成失败。

### 2. 数据质量问题

- URL 和标题去重主要发生在单次采集过程中，缺少跨天去重。
- 缺少事件级聚合，同一事件在不同媒体和不同日期可能重复出现。
- RSS 条目部分只有标题，没有摘要。
- 营销过滤词以英文为主，对中文和变体营销内容覆盖不足。
- A/B/C 评分是关键词和热度规则，尚未经过用户反馈校准。
- 日报没有强制展示事实、推测、传闻的区别。

### 3. AI 分析问题

- 日报使用固定 Prompt 和单次模型调用。
- 单次调用最大输出约 12,000 tokens，超时约 300 秒。
- 没有可靠重试、备用模型、Token 统计和结构化输出校验。
- 没有自动检查日期、来源、链接和事实是否来自素材。
- 实时资讯的影响分析主要是固定模板，不是独立的 AI 分析。
- 模型名、路由器地址和部分本地依赖被硬编码。

### 4. 稳定性和并发问题

- 日志显示多个日期被重复补录：2026-08-11 约 8 次，2026-08-12 约 5 次，2026-08-13 和 2026-08-14 各约 4 次。
- 多次启动应用或多个进程运行时，缺少跨进程任务锁。
- JSON 状态文件没有明确的原子写入和文件锁机制。
- 网络源出现 403、429、超时后，没有清晰地向用户展示来源失败状态。
- Flask 使用开发服务器，不适合直接承载多人访问。

### 5. 用户体验问题

- 用户看不到可靠的数据更新时间。
- 用户无法区分“今天没有新信息”和“系统更新失败”。
- 当前只有未读/已读，没有搜索、来源、分类和重要性筛选。
- 日报中的原文 URL 多数不是标准 Markdown 链接，点击体验不稳定。
- 实时采集状态和日报状态没有统一展示。

### 6. API、隐私和安全问题

- 服务监听 `0.0.0.0`，局域网内可以访问。
- 当前没有认证、授权和限流。
- 所有用户共享一个 `read_status.json`，不同用户会互相影响已读状态。
- `/api/reports` 返回全部日报，数据量增长后响应会变慢。
- Markdown 内容被作为安全 HTML 直接渲染，未来接入不可信内容时存在 XSS 风险。

### 7. 开源和维护问题

- Git 没有有效提交，当前主要文件都是未跟踪状态。
- 目录中没有源码工程，只有打包后的 EXE。
- 缺少 `requirements.txt`、`.env.example`、README、LICENSE、测试和构建脚本。
- 日志中保留了本机路径信息，应在开源前清理。
- 代理地址、路由器端口、Web 端口等配置没有统一配置管理。
- 当前没有发现常见格式的 API Key，但仍需在发布前做完整凭证扫描。

## 时效性解决方案

### 方案 A：启动时按天更新

用户打开应用时立即检查当天数据；已有日报先正常展示，采集和生成在后台执行；同一天只允许一个更新任务。

- 难度：低到中
- 稳定性：高
- 成本：低
- 实时性：分钟级
- 适合程度：最适合作为第一步

### 方案 B：Windows 定时任务

使用 Windows Task Scheduler 每 2～4 小时采集一次，日报每天最多生成一次。

- 难度：中
- 稳定性：高
- 成本：低到中
- 实时性：小时级
- 适合程度：适合桌面版稳定运行

### 方案 C：定时任务 + 启动兜底 + 文件锁

定时任务负责常规更新，应用启动负责补偿；使用日期锁和任务状态避免重复执行。

- 难度：中到高
- 稳定性：最高
- 成本：中
- 实时性：分钟级
- 适合程度：最终推荐方案

推荐顺序：先实现方案 A，再加入方案 B，最终形成方案 C。

## 优先级清单

### P0：必须解决

| 问题 | 解决措施 | 难度 | 预期收益 | 对现有功能影响 |
|---|---|---:|---:|---|
| 当天不会自动更新 | 启动时触发一次当天采集，后台异步生成 | 中 | 极高 | 不影响已有阅读 |
| 旧日报冒充今日 | 区分 `today`、`latest_available`、`stale` 状态 | 低 | 极高 | 只改善状态表达 |
| 日期和时区错位 | 统一北京时间/UTC 规则，校验条目日期范围 | 中 | 极高 | 不删除已有数据 |
| 补录重复执行 | 增加进程锁、日期锁和任务状态 | 中 | 高 | 保留补录功能 |
| AI 失败无反馈 | 显示采集成功、AI 生成失败和下一步建议 | 低 | 高 | 增强用户反馈 |
| 局域网无认证 | 默认本机访问；移动同步单独增加访问控制 | 中 | 极高 | 需兼容手机同步 |
| 无法复现构建 | 恢复源码、依赖、配置和打包脚本 | 中高 | 极高 | 不影响现有 EXE |

### P1：强烈建议解决

| 问题 | 解决措施 | 难度 | 预期收益 | 对现有功能影响 |
|---|---|---:|---:|---|
| 跨天和事件重复 | URL 规范化、内容指纹、事件级聚合 | 中 | 高 | 提高信息质量 |
| AI 幻觉和传闻风险 | 结构化中间结果、来源校验、事实等级 | 中 | 高 | 需兼容现有日报格式 |
| AI 成本偏高 | 规则预筛选、增量处理、输入长度限制 | 中 | 高 | 减少低价值内容 |
| 信源失败 | 独立超时、有限重试、熔断和失败标记 | 中 | 高 | 其他来源仍可继续运行 |
| 原文不可点击 | 统一生成带标题的来源链接 | 低 | 中高 | 改善阅读体验 |
| 缺少筛选 | 增加分类、重要性和来源筛选 | 中 | 中高 | 保留原浏览方式 |
| 全局已读状态 | 设计用户维度，单用户模式保持兼容 | 中 | 高 | 需要兼容旧 JSON |

### P2：有时间再解决

- API 分页、日期范围查询和摘要接口。
- 数据归档和保留期限配置。
- 采集成功率、来源失败率、耗时和 Token 监控。
- 采集、日期、去重、API 的回归测试。
- 统一日志编码和错误码。
- 链接健康检查。

### P3：暂时不要做

- PostgreSQL、Redis、向量数据库。
- 微服务、Kubernetes、复杂消息队列。
- 多 Agent 编排。
- 为当前数据规模设计企业级架构。

## 后续版本路线图

### V1.0：稳定且不误导

- 恢复源码工程。
- 实现当天自动采集。
- 修复日期和时区问题。
- 增加任务锁。
- 显示数据更新时间和失败状态。
- 保留现有 UI 和阅读流程。
- 补齐 README、依赖、配置和构建说明。

### V1.1：信息可信

- 跨天去重和事件聚合。
- 来源链接规范化。
- 事实、推测、传闻标记。
- AI 输出校验。
- 信源失败重试和降级。
- 增加基础测试。

### V1.2：高效阅读

- 搜索。
- 分类、重要性和来源筛选。
- API 分页。
- 收藏和用户级已读状态。
- Windows 定时任务。
- 采集缓存和数据归档。

### V2.0：可开源、可多人使用

- 配置化部署。
- 用户认证和权限。
- 独立采集 Worker。
- 独立 AI 生成任务。
- 数据库迁移方案。
- 任务队列和监控。
- 多用户成本控制。
- 可复现构建和发布流程。

## 后续实施原则

1. 先修复时效、日期和失败反馈，再扩展来源。
2. 实时采集和 AI 日报生成保持独立状态。
3. AI 不可用时仍展示最新原始资讯，但必须明确标记。
4. 不删除现有阅读、补录和手机同步功能。
5. 每次只做小范围增量修改，并进行回归验证。

