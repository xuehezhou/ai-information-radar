# AI 信息雷达开发规则

> 本文件是 AI 信息雷达项目的开发约束。
>
> 任何 AI 智能体进入项目后，必须先阅读本文件，再进行分析、设计、修改或测试。

## 1. 项目核心目标

AI 信息雷达的核心目标是帮助用户每天快速获得最新、有价值、可信、可理解的 AI 行业信息。

项目必须持续围绕以下结果改进：

- 信息更新及时。
- 信息质量可靠。
- AI 分析具有实际帮助。
- 系统运行稳定。
- 运行成本可控。
- 用户能够快速理解和使用。
- 项目未来可以安全开源和扩展。

所有开发工作都必须回答一个问题：

> 这次修改是否让用户更快、更准确地获得有价值的 AI 信息？

## 2. 核心开发原则

1. 先理解，再修改。
2. 先解决真实问题，再考虑代码优化。
3. 默认采用增量修改，不进行无必要的重构。
4. 不删除已有功能，不破坏已有流程。
5. 不擅自改变核心需求、数据流和用户体验。
6. 优先使用当前项目已有的技术、模块和依赖。
7. 每次修改都必须有明确原因、影响范围和验证方式。
8. 稳定性优先于新技术，实际收益优先于技术复杂度。

## 3. 收益优先原则

开发优先级必须按照用户收益判断，而不是按照功能看起来是否高级判断。

优先解决：

- 影响用户每天使用的问题。
- 影响信息准确性、时效性和可信度的问题。
- 影响系统稳定性和数据安全的问题。
- 高频使用且实现成本合理的问题。

降低优先级或暂缓：

- 只有展示效果、没有明确用户收益的功能。
- 当前数据规模完全用不到的企业级能力。
- 会增加维护成本但不能改善用户结果的技术方案。

## 4. 最小修改原则

每轮迭代只解决一个最高收益问题，尽量减少修改文件数量和逻辑变化范围。

执行要求：

- 只修改解决当前问题所必需的文件。
- 优先复用现有函数、接口和数据结构。
- 不为了代码风格统一而修改无关代码。
- 不为了“更漂亮”而重写模块。
- 不创建 `_v2`、`_new`、`_backup`、`_copy` 等副本文件。
- 需要回溯时使用 Git，而不是复制项目。

## 5. 修改代码前必须执行

修改前必须完成以下步骤：

1. 阅读本文件。
2. 阅读与当前任务相关的现有代码和文档。
3. 明确当前要解决的一个具体问题。
4. 说明问题对用户的实际影响。
5. 确认修改范围和不修改范围。
6. 评估是否会影响已有功能、数据和接口。
7. 比较至少一个更简单的可行方案。
8. 明确测试方法和成功标准。
9. 判断是否需要用户确认。

如果以上信息无法确认，不得直接修改代码。

## 6. 禁止擅自进行的操作

未经用户明确确认，禁止：

- 大规模重构架构。
- 更换技术栈或核心依赖。
- 删除已有功能、接口、数据和错误处理逻辑。
- 修改数据库 Schema 或执行破坏性数据操作。
- 修改 AI System Prompt 的安全约束。
- 修改模型、Token 上限、温度或计费相关参数而不说明影响。
- 引入企业级基础设施。
- 将真实 API Key、Token、密码写入代码、Prompt 或日志。
- 将未脱敏的隐私数据提交到版本库或输出给用户。
- 直接修改打包后的生产 EXE 代替修改源码。
- 将桌面快捷方式或默认启动入口指向旧 EXE；开发期入口必须指向主开发目录的 `start.bat`。

## 7. 功能新增原则

新增功能必须满足以下条件：

1. 能解决明确的用户问题。
2. 有清晰的使用场景。
3. 不破坏现有流程。
4. 不重复已有功能。
5. 有明确的验收标准。
6. 有合理的维护成本。

如果功能只增加复杂度，没有明确用户收益，应标记为“暂时不要做”。

## 8. AI 信息雷达核心优先级

项目优化优先级固定为：

1. 当天信息是否及时更新。
2. 信息是否真实、有效、低重复。
3. 用户是否能快速看懂最重要的信息。
4. AI 分析是否准确、有来源、有行动价值。
5. 系统是否稳定、可恢复、成本可控。
6. 项目是否容易维护、开源和扩展。

不得为了增加信源数量、页面数量或 AI 能力而降低以上优先级。

## 9. AI 调用原则

AI 调用必须遵守：

- 只把必要的信息发送给模型。
- 先去重、过滤和压缩，再调用模型。
- 避免对相同内容重复调用。
- 为请求设置超时、有限重试和失败状态。
- 记录模型、耗时、输入规模、输出规模和失败原因。
- 对 AI 输出进行格式、来源、日期和链接校验。
- 对传闻、推测和未经证实的信息明确标注。
- AI 不可用时，系统仍应尽可能展示已经采集的原始信息。
- 不把 AI 生成内容默认当作事实。
- 不在 Prompt 中硬编码任何敏感凭证。

## 10. 数据原则

数据处理必须保证：

- 来源可追溯。
- 采集时间、原始发布时间和日报日期清晰区分。
- 使用统一时区规则。
- URL、标题和内容具有稳定去重标识。
- 数据写入尽量原子化，并避免并发覆盖。
- 原始数据和处理后数据职责清晰。
- 不因为一次采集失败而删除已有有效数据。
- 不未经确认清理、覆盖或删除用户数据。
- 数据文件格式和字段变化必须记录。

## 11. 实时性原则

用户打开 AI 信息雷达时，应优先看到当天或最近 24 小时内的重要信息。

实时更新必须遵守：

1. 用户打开页面不应被长时间阻塞。
2. 更新任务应支持后台执行。
3. 同一天不应重复执行相同任务。
4. 采集失败、AI 失败和没有新信息必须区分。
5. 旧数据可以继续展示，但必须明确标注更新时间和过期状态。
6. 实时采集和 AI 日报生成可以独立失败、独立恢复。
7. 日报生成应设置合理频率，避免重复消耗 Token。

## 12. 用户体验原则

用户体验必须围绕“快速获得价值”设计：

- 用户打开后应立即知道系统是否正在更新。
- 用户应能区分今天、最近一天和历史信息。
- 用户应能看到信息来源和原文入口。
- 用户应能理解 AI 结论与原始事实的区别。
- 错误信息必须说明原因和下一步建议。
- 不因一次失败操作导致整个流程中断。
- 重要信息优先展示，避免无意义的信息过载。
- 不改变现有 UI，除非 UI 问题直接阻碍核心用户目标。

## 13. 稳定性原则

所有关键流程都必须考虑异常情况：

- 网络断开。
- 数据源失效或限流。
- AI 服务超时或不可用。
- 路由器未启动。
- 文件损坏或写入失败。
- 端口被占用。
- 用户连续点击或重复刷新。
- 多个进程同时运行。

稳定性要求：

- 单个信源失败不能拖垮全部信源。
- 单次 AI 失败不能破坏已有日报。
- 任务必须具备幂等性或重复执行保护。
- 错误必须记录可读日志。
- 系统必须保留可用的旧数据作为降级结果。

## 14. Debug 原则

遇到 Bug 必须按照以下顺序处理：

1. 记录用户操作路径。
2. 复现问题。
3. 收集错误信息、日志和环境信息。
4. 定位具体模块和根因。
5. 分析影响范围。
6. 提出最小修复方案。
7. 修改代码。
8. 执行回归测试。
9. 记录修复结果和剩余风险。

禁止通过猜测直接修改代码。

## 15. 测试原则

每次代码修改后必须进行与风险匹配的测试。

至少覆盖：

- 正常流程。
- 失败流程。
- 边界条件。
- 重复操作。
- 相关旧功能回归。
- 涉及可见页面时，验证 8899 运行的是主源码实例，而不是旧 EXE 或旧进程。

涉及数据流、API、采集、AI 调用或并发时，应增加对应的集成测试或手动验收。

不能运行测试时，必须说明原因、未验证内容和残余风险，不得声称“已验证通过”。

## 16. 每轮迭代结束必须输出

每轮修改结束后，必须说明：

- 修改了什么。
- 为什么修改。
- 修改了哪些文件。
- 哪些文件没有修改。
- 影响了哪些功能。
- 测试了什么。
- 测试结果是什么。
- 是否影响已有功能。
- 还有哪些已知问题。
- 本轮带来的实际用户收益。
- 下一轮最值得解决的问题是什么。

完成一轮后不得自动继续修改下一轮问题，必须等待用户确认。

## 17. 初学者解释原则

当用户需要解释时，应使用小白能够理解的方式：

- 先讲结论，再讲原因。
- 说明数据从哪里来、经过什么处理、最后展示在哪里。
- 解释每一步为什么存在。
- 减少不必要的专业术语。
- 使用简单示例、流程图或表格。
- 不默认用户了解隐藏的技术细节。

## 18. 开源安全原则

项目准备开源前必须检查：

- API Key、Token、密码和私密 URL。
- 本机路径、用户名和个人信息。
- 运行日志和调试数据。
- 用户数据和已读状态。
- 本地代理、路由器和内部服务地址。
- 依赖许可证和第三方资源授权。
- EXE 中是否包含不应公开的代码或配置。

发现敏感信息时，先停止发布流程并告知用户。

## 19. 开源兼容原则

开源版本必须尽量具备：

- README 和启动说明。
- `requirements.txt` 或等效依赖清单。
- `.env.example` 或配置示例。
- `.gitignore`。
- LICENSE。
- 可复现的构建和运行步骤。
- 示例数据或空数据启动方式。
- 数据格式和配置说明。
- 基础测试和故障排查说明。

不得把只适用于个人电脑的绝对路径、隐藏服务或私有配置作为默认依赖。

## 20. 版本迭代原则

项目采用小版本持续迭代：

- 每轮只解决一个高收益问题。
- 每轮保留已有功能。
- 每轮记录变更、测试和已知风险。
- 需要破坏性变更时，必须提前说明并获得确认。
- 优先提供可运行、可验证的小版本。
- 不为了版本号而堆积功能。

推荐迭代顺序：

```text
稳定基础功能
    ↓
解决时效性
    ↓
提高信息质量
    ↓
提高 AI 分析可靠性
    ↓
改善高频阅读体验
    ↓
支持开源和多人扩展
```

## 21. “暂时不要做”原则

当一个需求当前没有明确收益、实现成本过高或会增加不必要复杂度时，必须明确告诉用户：

> 暂时不要做。

以下情况应优先暂缓：

- 当前用户用不到的企业级架构。
- 没有真实数据支撑的个性化推荐。
- 不能改善核心信息价值的装饰功能。
- 尚未解决时效性和数据质量前的大规模信源扩张。
- 还没有源码和测试基础时的复杂重构。
- 仅为了使用新技术而引入的新技术。

暂缓不是删除需求，而是记录原因，等待更合适的阶段。

## 22. 最重要的总规则

所有技术决策必须遵守以下优先级：

```text
用户价值 > 功能数量

稳定性 > 复杂度

简单方案 > 复杂方案

实际收益 > 技术炫技

小步迭代 > 一次性重构
```

如果技术方案与以上原则冲突，必须优先选择更稳定、更简单、更有实际收益的方案。

## 23. AI 智能体执行前检查

每次准备修改代码之前，必须检查：

- [ ] 我是否理解了当前需求？
- [ ] 我是否阅读了本文件？
- [ ] 我是否知道要解决的具体问题？
- [ ] 这个问题是否具有明确用户收益？
- [ ] 我是否只修改必要文件？
- [ ] 是否会影响已有功能？
- [ ] 是否存在更简单的方案？
- [ ] 是否需要用户确认？
- [ ] 修改后如何测试？

如果无法回答以上问题，不要直接修改代码。

## 24. 最终开发理念

```text
Build small.
Test often.
Improve based on value.
```

小步开发。

频繁测试。

根据真实收益持续迭代。

不要一次做完。

不要过度设计。

不要擅自扩大需求。

每一次修改，都必须有明确理由。
